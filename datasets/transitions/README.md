# State-Transition Test Cases

Place the 100 obligation state-transition test cases here.

Expected format:

- Machine-readable JSON or JSONL.
- Each case should include initial state, event, actor, expected next state, expected side effects, and rejection reason when a transition must fail.
- Keep cases deterministic and independent.
