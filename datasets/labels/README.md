# Labelled Extraction Results

Place human-reviewed extraction labels here.

Expected format:

- JSON files keyed to contract PDFs by base file name.
- Include extracted obligations, source anchors, page references, line references where available, confidence annotations, and reviewer metadata.
- Labels should represent expected truth data, not model output.
