# Reminder Scenarios

Place the 20 restart-safe reminder scheduling scenarios here.

Expected format:

- Machine-readable JSON or JSONL.
- Each scenario should include obligations, due dates, reminder policy, simulated clock timestamps, expected jobs, and expected audit events.
- Include restart and duplicate-prevention cases.
