# API Documentation

This directory documents HTTP API contracts, authentication behavior, error formats, and versioning conventions.

Shared request and response types should live in `packages/shared` when they are used by more than one workspace.
